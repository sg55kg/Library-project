let myLibrary = [];

function addBookToLibrary() {
        addBookButton = document.getElementBy("addBook");
        addBookButton.addEventListener('click', function() {

        let getTitle = document.getElementBy("getTitle").value;
        let getAuthor = document.getElementBy("getAuthor").value;
        let getInfo = document.getElementBy("getInfo").value;

        

     });
}
